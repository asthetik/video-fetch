{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window only. The bilibili-login window loads an external page and needs no IPC permissions; downloads spawn yt-dlp/ffmpeg from Rust (tokio::process::Command), so no shell:allow-execute is required.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
