# Changelog

本项目的版本说明集中记录于此，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed

- 顶部导航的选中态由「两个按钮的珊瑚底色交叉淡入淡出」改为「一个珊瑚指示块在按钮之间滑动」：切换菜单时选中态读作一个色块移动过去，而不是原地闪两下（交叉淡入时中间会有一帧两个半透明珊瑚底色同时存在，看起来像选中被撕成两半）。滑动 160ms、`ease-in-out`，取 DESIGN.md 微交互 90–160ms 带的上限。
- 导航文字变白的时机随之对齐指示块：此前文字在点击瞬间就开始变白，而指示块要 160ms 才滑到，中间约 70ms 里白字直接压在宣纸底上（1.07:1，等于看不见）。现在未选中项快速回到次级墨（90ms、`ease-in`），选中项等指示块滑到位再变白（90ms、`ease-out`、延迟 70ms）。这套时序按相邻按钮的 80px 间距标定：相邻切换两个方向都不会出现文字压在纸底上；跨多个按钮时指示块要用同样的 160ms 走更长的距离、扫过标签更快，被离开与被选中的标签在移动中段仍各有约 45ms 对纸底低于 3:1（最低 1.21:1），比改动前全程 1.07:1 好得多但并未消除。偏好减少动态效果时，指示块与文字配色都瞬间到位。
- 日志页 INFO/WARN/ERROR 徽章重新配色：INFO 不再用与主按钮同色相的珊瑚 tint，改为中性灰底（`--muted-tint`）＋正文墨，常态级别不再读作主操作色；WARN 与 ERROR 新增强档 token（`--warn-tint-strong`／`--danger-tint-strong` 底，配 `--warn-ink-strong`／`--danger-ink-strong` 字），底色加浓、浅色墨各深一档，比状态胶囊更醒目。四个徽章在浅色与深色主题下文字对底均 ≥ 4.5:1（WCAG AA），对比度守卫测试改为对准 `.log-view` 实际渲染徽章的 `--ctl-bg` 背景并覆盖新 token。
- 「关于」页的作者、版本、许可证、源码四行改为整行可点的卡片：点击分别打开作者 GitHub 主页、当前版本的发行说明（`releases/tag/v{版本}`）、许可证全文与仓库首页，卡片右侧标出去向说明（GitHub 主页 / 发行说明 / 许可证全文 / GitHub 仓库）并配外链图标，让去处一眼可见；悬停、键盘聚焦（取粉 2px 环）与按下均有状态反馈，源码行的展示值由完整 URL 收敛为 `asthetik/video-fetch`。
- 构建与测试所用的 Python 由 3.12 升级到 3.14.7：版本号集中固定在新文件 `.python-version`，CI（frontend 之外的 rust / scripts 两个 job）与发布流程的三处 `setup-python` 统一改为读取该文件，今后升级只需改这一处。
- 侧载脚本的 tar 解压改为显式声明 `filter="data"`：Python 3.14 已把该过滤器变成默认值，显式传参让路径穿越防护不再随解释器版本变化（3.12–3.14 行为一致，更老的解释器会直接报错而非无防护解压）；同时 Linux 分支的解压目标改为独立子目录，不再写回压缩包自身所在目录，并补齐了对应的单元测试。
- 状态/日志徽章与提示文字通过 WCAG AA 对比度复检：每个语义色新增专用「墨阶」（`--accent-ink`、`--warn-ink`、`--danger-ink`、`--success-ink`、`--muted-ink`、`--celebrate-ink`），这些文字一律走墨阶，中间调只做填充、描边与图标；浅色主题此前有 13 条 10–16px 文字规则不达标（最差的 Hi-Res 徽章只有 1.6:1 量级），现已达标，墨阶按各自真实背景求解后落在 4.54–6.05:1 区间（例外：警告徽章的 tint 若落在纸底画布上为 4.34:1，故警告徽章固定在卡片表面上使用）。**按钮与焦点环不在本次复检范围内**，其配色保持原样。
- 焦点环与品牌色不加深：焦点环继续用取粉 #FB7299（原来用珊瑚的三处焦点环也仍是珊瑚），空间列表选中行左侧竖条也恢复为取粉，不引入深色取墨。代价是取粉焦点环在白底 2.64:1、纸底 2.47:1，低于焦点指示的 3:1 门槛，作为已知偏差保留。`.space-search` 仍由 `:focus` 改为 `:focus-visible`（只改触发条件，不改颜色）。
- 按钮配色整体保持原样，覆盖填充、描边、文字与悬停：菜单（导航选中态）与主按钮用珊瑚 #F2596F／悬停 #D8435C、次级按钮悬停文字珊瑚、文字按钮用取粉 #FB7299／悬停 #E05C80、危险按钮用砖红 #D6453F、顶栏登录状态按钮用松绿 #2F9E6E／琥珀 #D98A2B、图标按钮与选中的筛选 chip 同样照旧。中途曾用墨阶与 `-strong` 深色实底去提升这些按钮的对比度，按「按钮配色不动」的决定全部回退；白字压珊瑚 3.26:1（深色 2.74:1）、危险按钮悬停 4.39:1（深色 3.46:1）、文字按钮取粉 2.64:1 等作为已知偏差保留。
- 卡片与输入框表面由 #FFFDF8 改回纯白，宣纸底 #FAF7F4 重新拉开层级（通道差由 6 回到 11）；表单控件补 `font: inherit`，让按钮与下拉框真正使用内置 MiSans，而不是各平台的 UA 默认字体。
- 状态 chip 底色全部收敛为 token：四个彩色 tint 保持原有明度阶梯（对白底均为 1.15–1.16:1），灰色 chip 改用新增的 `--muted-tint` / `--muted-tint-soft`，因此 retire 掉两条深色模式覆盖规则。其中浅色警告徽章的底色由 rgba(255, 204, 0, 0.2) 校正为琥珀色相的 rgba(217, 138, 43, 0.16)，这是一处可见的视觉调整。
- Hi-Res 徽章底色补 token 化为 `--celebrate-tint`（浅色 0.16／深色 0.18，深色覆盖规则随之移除），前条「chip 底色全部收敛为 token」至此完整成立；深色错误徽章文字色随墨阶统一由 #E26060 微调为 #F07070（补记这处可见变更）。

### Fixed

- 顶部导航按钮补上按压态：`DESIGN.md` 要求交互元素齐备 hover / focus-visible / active / disabled 四态，此前导航按钮只有前两态。未选中项按下时底色加深到 `--space-selected`，选中项按下时指示块加深到 `--accent-hover`（与主按钮悬停同色；白字压珊瑚由 3.26:1 升到 4.39:1）。用色阶而不用 `.btn` 家族的 `transform: scale()`：导航按钮内含绝对定位的滑动指示块，按钮自身一旦有 transform 就会变成层叠上下文，在按下瞬间改变它与指示块的绘制次序。导航按钮没有禁用场景，第四态不适用。
- 修复切换主题时原生标题栏不同步的两处问题：从「深色」切回「跟随系统」时，标题栏要等一会儿才变回白色；从「浅色」切到「深色」时，标题栏又一直停留在白色。现在每次应用主题都会同步设置所有窗口的原生外观（含登录窗口，窗口在创建时即应用当前档位）——固定档直接钉住对应外观，「跟随系统」则交还系统控制。窗口改为在主题应用完成后才显示，冷启动不再先闪一下系统外观；「深色 → 跟随系统」在原生外观清除后立即重解一次系统偏好，页面不再等媒体查询事件才变浅；重复应用同一档位不再重复下发；固定档在开启「提高对比度」时改用对应的高对比外观变体。已知限制：macOS 26+ 会推迟标题栏的重绘，外观状态立即生效但视觉上要等下一次页面导航（或窗口大变更）才完成刷新；旧系统的行为不变。
- 深色模式下 `DEBUG` 日志徽章此前没有任何配色规则，会以裸样式渲染，现补上中性徽章样式。

## [0.4.0] - 2026-09-19

### Changed

- 应用图标全新设计「落定」：暖宣纸底上一张微倾的取粉卡片，卡面切出指向下方的播放三角（播放与落地合一），身后一抹纯平墨色投影——「影」取，影像与它的影子一起取走。全新图标覆盖全部平台（macOS / Windows / Linux 安装包与任务栏、应用内网页 favicon、安卓与 iOS 图标资源），README 头图同步更换；安卓自适应图标背景定为墨色，浅色桌面下同样清晰可辨。
- 界面视觉整体切换到「纸墨」品牌体系：宣纸底（#FAF7F4）与墨色文字（#221F26）取代系统冷灰；主操作与进度用珊瑚色（#F2596F），焦点环与文字链接用取粉（#FB7299），琥珀（#FFB042）只保留给 Hi-Res 品质徽章；深色模式改为夜幕/夜卡双层表面（#17151C / #211E27）；卡片圆角 18px、控件 10px。
- 正文字体内置 MiSans：按「GB2312 全集 + 界面字符」子集打包四个实际使用字重（约 3.7MB），视频标题、UP 主名等任意中文在三端渲染一致，不再依赖各平台系统字体；第三方组件声明新增字体段落（MiSans 嵌入署名、得意黑与 JetBrains Mono 许可信息）。
- 更新 npm 与 Cargo 依赖。

## [0.3.4] - 2026-09-12

### Added

- 「关于」页第三方组件区块显示 yt-dlp 与 ffmpeg 的 release 固定版本：编译期从版本固定文件（`requirements-sidecars.txt` / `FFMPEG_VERSION`）注入，与发版包实际捆绑的二进制同源一致，展示零子进程、零等待。

### Changed

- 发布打包的 yt-dlp / ffmpeg sidecar 由「取构建时最新」改为固定版本：yt-dlp 固定于 `scripts/requirements-sidecars.txt`（Dependabot pip 生态每月自动升级），ffmpeg 固定于 `scripts/fetch_sidecars.py` 的 `FFMPEG_VERSION`（当前 9.0.1，手动升级；Linux/Windows 用 BtbN 对应 release 分支构建，macOS 用 evermeet 版本直链）。CI sidecar 缓存 key 纳入版本文件，版本变更自动失效重下。
- 更新 npm 与 Cargo 依赖。

## [0.3.3] - 2026-09-05

### Changed

- 日志文件改为单文件滚动：活跃文件固定为 `app.log`（2 MiB，写满轮转为 `app.old.log`），不再按天切分产生碎片文件；历史按天文件在 30 天保留期内自然老化，无需迁移。
- 日志只记录关键事件（启动、下载生命周期、破坏性操作、警告与错误）：移除页面导航、自动点击、粘贴等前端埋点与解析成功记录；过滤器固定为 `info`，第三方依赖（reqwest/hyper）的 DEBUG 噪音不再写入。
- 更新 npm 与 Cargo 依赖。

## [0.3.2] - 2026-08-29

### Added

- 「UP 主空间批量下载」：粘贴 `space.bilibili.com/{mid}` 链接即列出该 UP 主全部投稿视频（每页 50、「加载更多」）；支持最新发布/最多播放排序、空间内关键词搜索；跨页、跨搜索保留勾选，统一档位批量入队（视频：最高/1080P/720P/480P；仅音频：m4a/mp3/FLAC），完成后展示「已入队 / 跳过已存在 / 队列重复 / 失败」汇总与失败明细；选中超过 50 个时先弹确认。
- 空间列表主路径（WBI 签名 arc/search）失败时自动降级 yt-dlp flat-playlist；降级模式下搜索与排序禁用并明确提示，不静默返回错数据。

### Changed

- 下载入队内部改为结构化 `EnqueueKind` 分类，单视频下载的提示文案与行为保持不变。
- 刷新 npm 与 Cargo 依赖。

## [0.3.1] - 2026-08-23

### Added

- 「仅音频下载」：视频卡片新增「视频 / 仅音频」切换；仅音频模式可选音质档位（64/132/192kbps AAC、Hi-Res 无损）与输出格式（m4a / mp3 / FLAC，FLAC 仅 Hi-Res 源可选），mp3/FLAC 走 ffmpeg 转码。

### Changed

- 同一稿件的视频下载与仅音频下载互相独立：可以各下一份，也不会因对方已存在而报冲突。
- Windows x64 安装包文件名带 `-x64` 后缀（`Video-Fetch-v*-Windows-x64.msi` / `.exe`），与 arm64 包区分。

### Fixed

- 音频档位在下载时对照 yt-dlp 实际格式表固定到精确音轨，不再出现「选 64kbps 却下载 Hi-Res」的错位（含 playurl 带宽取整与 yt-dlp abr 浮点的舍入差异）。
- 仅音频下载不再回退到视频流：无可用音轨时明确报错，而不是静默下载视频再提音频。
- FLAC 校验：非 Hi-Res 档位拒绝 FLAC；无无损音源的视频下载 FLAC 时报错，不再静默转码出「伪无损」。
- Hi-Res 固定选 FLAC：即使同稿 Dolby 码率更高，也不会把无损档挤成带 `abr` 上限的有损轨。
- 多 P 抽样合并音质时，任一集有 Hi-Res 则保留 Hi-Res 档位与文案。
- 数据库迁移先检查 `audio_format` 列是否存在再执行 ALTER，真实的失败不再被静默吞掉。
- 无码率的音轨条目不再显示为「0kbps」档位。
- 同名不同容器的音频文件不再互相冲突：已有 `.m4a` 不会挡住新的 `.mp3`，另存副本编号只统计同容器文件。
- 仅音频模式解析失败时显示错误提示与重试按钮；FLAC 可用性改用结构化标志，不再依赖档位文案判断。

## [0.3.0] - 2026-08-23

### Changed

- 登录凭证不再使用系统钥匙串（#54）：未签名 macOS 构建不再弹出「登录钥匙串」密码框，凭证只保存在应用数据目录的本地文件（仅当前用户可读）。

### Fixed

- 凭证写入改为原子替换（临时文件 + 重命名），登录/导入期间崩溃也不会留下损坏文件；损坏文件会按未登录状态降级，重新登录即可恢复。

### Changed

- README 重写：面向新手的安装与使用说明，补充本地日志与数据存储位置说明。
